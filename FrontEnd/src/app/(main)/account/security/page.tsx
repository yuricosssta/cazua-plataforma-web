import { EmailsAndAuth } from "@/components/dashboard/EmailsAndAuth";

export default function SecurityPage() {
  return (
    <div className="w-full h-full flex flex-col">
      <EmailsAndAuth />
    </div>
  );
}