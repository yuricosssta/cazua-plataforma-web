{
  "name": "cazua-tenant-router",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker para roteamento de subdomínios multi-tenant do Cazuá",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:root": "wrangler secret put ROOT_DOMAIN",
    "secret:app": "wrangler secret put APP_DOMAIN",
    "secret:vercel": "wrangler secret put VERCEL_ORIGIN",
    "secret:purge": "wrangler secret put PURGE_TOKEN",
    "secret:cf_token": "wrangler secret put CF_API_TOKEN"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260826.1",
    "wrangler": "^4.129.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
