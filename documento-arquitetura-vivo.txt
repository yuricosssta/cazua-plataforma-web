=== SPRINT 26 - 17/08/2026 ===
Resumo Técnico de Fechamento
1. Decisões de Arquitetura (BFF, Redux, EDA, Repositories)

Segurança e Tenant Isolation (NestJS): Refatoração da camada de autorização (AuthGuard e TenantGuard). O organizationId passou a ser extraído do cabeçalho x-org-id e validado contra a coleção OrganizationMember. A injeção do ID no objeto request previne Insecure Direct Object Reference (IDOR) nas rotas de manipulação da landing page.

Padrão Repository e Regras de Negócio: Delegação absoluta das chamadas Mongoose ao LandingPageConfigRepository. O LandingPageConfigService orquestra a regra de negócio implementando a validação de colisão de domínios atrelados a organizações distintas (findConflictingDomain) antes de autorizar o upsert.

Type Safety vs Mongoose: Resolução de erro de compilação (TS2345) gerado pela avaliação shallow do tipo utilitário Partial<T> no TypeScript em relação a propriedades aninhadas (theme). Aplicação de Type Assertion explícito para garantir a persistência correta via operador $set.

2. Definições de Infraestrutura (Variáveis, Rotas, UI)

Rotas de API Mapeadas e Seguras: Consolidação dos endpoints privados GET /landing-pages/me e PATCH /landing-pages/me no LandingPageConfigController, protegidos simultaneamente por validação de sessão (JWT) e validação de pertinência à organização.

Mitigação de Runtime Exceptions: Implementação de checagem estática Types.ObjectId.isValid() diretamente na barreira do TenantGuard, bloqueando payloads com IDs malformados antes de consumirem processamento nas camadas de Controller e Service.

Preparação para Fase 2: O ecossistema NestJS está isolado, validado e preparado para receber as requisições de proxy do Route Handler (BFF) do Next.js na próxima iteração, mantendo o tráfego do front-end seguro.

=== SPRINT 25 - 17/08/2026 ===
### Resumo Técnico de Fechamento

**1. Decisões de Arquitetura (BFF, Redux, EDA, Repositories)**

* **Roteamento Multi-tenant (Edge Middleware):** Implementação centralizada no Next.js (`middleware.ts`) interceptando o cabeçalho `host` para reescrita transparente das rotas públicas (`/sites/[domain]`), preservando a URL do cliente.
* **Isolamento de Estado (Redux):** Estado global restrito aos módulos `(main)` e `(auth)`. A camada pública de landing pages opera puramente com Server Components (RSC) e estado local efêmero nos Client Components (ex: `<LeadCaptureForm/>`).
* **Padrão Repository (Backend NestJS):** Isolamento absoluto na camada de acesso a dados. O `LandingPageConfigRepository` e o `LeadRepository` encapsulam as chamadas ao Mongoose. Mutação de estado via Soft Delete (`isActive: false`).
* **Event-Driven Architecture (EDA):** Emissão de eventos assíncronos (`lead.created` via `EventEmitter2`) no `LeadService` para gerenciar notificações externas de forma desacoplada do fluxo de resposta HTTP.

**2. Definições de Infraestrutura (Variáveis, Rotas, UI)**

* **Validação e Segurança:** Uso obrigatório de Zod end-to-end (`tenantLandingPageSchema`). Validação de domínios e injeção do `tenantId` encapsulada no servidor.
* **Motor MDX (next-mdx-remote):** Renderização no servidor de conteúdos customizados pela construtora, provendo um dicionário restrito de componentes React mapeados, mitigando riscos de XSS.
* **UI/UX e Tailwind v4:** Conversão algorítmica de Hexadecimal para HSL no componente `LandingPageSettings.tsx` durante o `onSubmit`. Injeção dinâmica de propriedades CSS nativas (`--primary`) na raiz da landing page, mantendo estrita compatibilidade com classes semânticas (`bg-background`, `text-foreground`, `rounded-md`) e Dark Mode nativo.

---

### Mapa Completo de Arquivos do Front-end

```text
FrontEnd/src$ ls -R
.:
app  components  lib  providers  types  validations

./app:
(auth)  (main)   api   globals.css   layout.tsx   page.tsx   robots.ts   sitemap.ts   sites

./app/(auth):
forgot-password  login  reset-password  signup

./app/(auth)/forgot-password:
page.tsx

./app/(auth)/login:
page.tsx

./app/(auth)/reset-password:
page.tsx

./app/(auth)/signup:
page.tsx

./app/(main):
account  dashboard  layout.tsx

./app/(main)/account:
layout.tsx  organization  profile  security  settings

./app/(main)/account/organization:
page.tsx

./app/(main)/account/profile:
page.tsx

./app/(main)/account/security:
page.tsx

./app/(main)/account/settings:
page.tsx

./app/(main)/dashboard:
layout.tsx  marketing  master-admin  page.tsx  people  planning  posts  projects  resources  settings  storage

./app/(main)/dashboard/marketing:
reels

./app/(main)/dashboard/marketing/reels:
page.tsx

./app/(main)/dashboard/master-admin:
page.tsx

./app/(main)/dashboard/people:
page.tsx

./app/(main)/dashboard/planning:
page.tsx

./app/(main)/dashboard/posts:
[id]   new   page.tsx

./app/(main)/dashboard/posts/[id]:
edit  page.tsx

./app/(main)/dashboard/posts/[id]/edit:
page.tsx

./app/(main)/dashboard/posts/new:
page.tsx

./app/(main)/dashboard/projects:
[id]   page.tsx

./app/(main)/dashboard/projects/[id]:
page.tsx

./app/(main)/dashboard/resources:
page.tsx

./app/(main)/dashboard/settings:
page.tsx

./app/(main)/dashboard/storage:
page.tsx

./app/api:
axiosInstance.ts  posts  summary

./app/api/posts:
[id]   route.ts

./app/api/posts/[id]:
route.ts

./app/api/summary:
reels  text

./app/api/summary/reels:
generate

./app/api/summary/reels/generate:
route.ts

./app/api/summary/text:
route.ts

./app/sites:
[domain]

./app/sites/[domain]:
page.tsx

./components:
AdmPost.tsx            Footer.tsx             Spinner.tsx              UserNav.tsx       color-mode-switcher.tsx  layout    resources
AudioTranscriber.tsx  LandingPageHeader.tsx  TextSummarizer.tsx       UserProfile.tsx  dashboard                planning  summary
ClientOnly.tsx        LogoBloco.tsx          TranscriptionModal.tsx  auth              landing                  posts     ui

./components/auth:
SessionExpiredModal.tsx  SessionWarningModal.tsx

./components/dashboard:
ActivityLog.tsx              DashboardMetrics.tsx  ExportPdfModal.tsx    OrgSwitcher.tsx            PersonalTasksPanel.tsx   TimelineEventCard.tsx
CreateOrganizationModal.tsx  EmailsAndAuth.tsx      ManageTeamDrawer.tsx  OrganizationPeople.tsx    ProjectsList.tsx         UpgradeModal.tsx
CreateProjectModal.tsx       EmitParecerModal.tsx  MyProfile.tsx          OrganizationSettings.tsx  TabPreferencesModal.tsx  settings

./components/dashboard/settings:
BrandingSettings.tsx  DataManagement.tsx  LandingPageSettings.tsx  StorageManagement.tsx

./components/landing:
ComercialLogin.tsx  FeatureSection.tsx  HeroSection.tsx  LeadCaptureForm.tsx  PricingSection.tsx

./components/layout:
Header.tsx  SettingsSidebar.tsx  Sidebar.tsx

./components/planning:
SearchCompositionsModal.tsx  UploadCostsModal.tsx  UploadPlanningModal.tsx

./components/posts:
BlogPostContent.tsx  MarkdownPreview.tsx  PostForm.tsx  PostList.tsx

./components/resources:
AddStockModal.tsx          EditResourceModal.tsx          ProjectCostSummary.tsx   RequestResourceModal.tsx  TransactionHistory.tsx
AllocateDirectlyModal.tsx  ManageWarehouseTeamDrawer.tsx  ProjectCostTab.tsx       ResourceCatalog.tsx
CreateResourceModal.tsx    PendingRequests.tsx            ProjectResourcesTab.tsx  ReturnResourceModal.tsx

./components/summary:
ReelGenerator.tsx

./components/ui:
MapViewer.tsx  aspect-ratio.tsx  badge.tsx  checkbox.tsx        input.tsx  separator.tsx  textarea.tsx
alert.tsx      avatar.tsx        card.tsx   dropdown-menu.tsx  label.tsx  sheet.tsx

./lib:
api  date.ts  markdownExample.ts  redux  services  utils.ts

./lib/api:
serverUtils.ts

./lib/redux:
slices  store.ts

./lib/redux/slices:
authSlice.ts  organizationSlice.ts  planningSlice.ts  postsSlice.ts  resourcesSlice.ts  userSlice.ts

./lib/services:
organizationService.ts  postService.ts     resourceService.ts  summaryService.ts        userService.ts
planningService.ts      projectService.ts  storageService.ts   transcriptionService.ts

./providers:
AuthInitializer.tsx  ReduxProvider.tsx  ThemeProvider.tsx

./types:
organization.ts  post.ts  project.ts  user.ts

./validations:
post.zod.ts  summary.zod.ts  tenant.zod.ts

```

=== SPRINT 24 - 16/08/2026 ===
### Resumo Técnico de Fechamento

**1. Decisões de Arquitetura (BFF, Redux, EDA, Repositories)**

* **Roteamento Multi-tenant (Edge Middleware):** Implementação de `middleware.ts` no Next.js para interceptação via cabeçalho `host`, reescrevendo requisições de domínios customizados para a rota estática `/sites/[domain]/...` sem alterar a URL no navegador.
* **Padrão Repository Estrito:**
* Criação de `LandingPageConfigRepository` e `LeadRepository` no NestJS, isolando completamente o acesso aos models do Mongoose da camada de Service.
* Uso mandatório e tratamento explícito de `new Types.ObjectId(id)` na entrada dos dados nos Services.
* Aplicação de deleção lógica (`isActive: false`) na gestão da visibilidade das landing pages.


* **Event-Driven Architecture (EDA):** Configuração do módulo de Leads (`LeadService`) para emitir o evento `lead.created` assincronamente via `EventEmitter2`. Isso desacopla a resposta HTTP ao usuário final do processamento pesado (disparo de webhooks ou e-mails para a construtora).
* **Isolamento de Estado (Redux):** Manutenção do Redux restrito ao ambiente logado. O roteador público opera via Server Components (RSC) otimizando SEO, consumindo estado efêmero apenas no formulário de client-side (`LeadCaptureForm`).

---

**2. Definições de Infraestrutura (Variáveis, Rotas, UI)**

* **Variáveis e Rotas:**
* Injeção e configuração da variável de ambiente `NEXT_PUBLIC_ROOT_DOMAIN`.
* Estruturação da rota interceptadora no Front-end: `app/sites/[domain]/page.tsx`.
* Mapeamento das rotas API no NestJS: `POST /leads` (público) e `GET/PATCH /landing-pages/me` (privado/dashboard).


* **Motor de Componentes Dinâmicos (MDX):**
* Adoção de `next-mdx-remote/rsc` no Next.js para renderização Server-Side de conteúdos customizados pelas construtoras.
* Mapeamento seguro de injeção de componentes: O componente `<LeadCaptureForm/>` é injetado programaticamente via dicionário MDX recebendo o `tenantId` encapsulado pelo servidor, mitigando vulnerabilidades de XSS (Cross-Site Scripting).


* **UI/UX e Validação Estrita (Zod):**
* Validação end-to-end (Client/Server) utilizando schemas centralizados do Zod (`landingPageFormSchema`, `createLeadSchema`).
* O componente de configuração (`LandingPageSettings.tsx`) foi construído convertendo valores Hexadecimais matematicamente para HSL (`h s% l%`) no momento do submit, mantendo a compatibilidade obrigatória com as variáveis semânticas do Tailwind v4 (`--primary`, `bg-background`).
* Remoção do uso do `.default(true)` no schema Zod integrado ao React Hook Form, ajustando a inferência de tipos e eliminando conflitos de controle de estado do React.


=== SPRINT 23 - 11/08/2026 ===
Resumo Técnico de Fechamento
1. Decisões de Arquitetura

Mitigação de Segurança (Front-end): Intervenção direta na árvore de dependências transitivas para sanar a vulnerabilidade de negação de serviço (DoS) vinculada a versões inseguras do nanoid.

Estabilidade de Build: Aplicação de sobrescrita de versão (override) no gerenciador de pacotes, garantindo a atualização do pacote vulnerável sem forçar upgrades majoritários não testados no Next.js, Tailwind e PostCSS. Essa decisão preserva a compatibilidade de módulos (evitando conflitos CJS/ESM).

2. Definições de Infraestrutura

Manifesto: Atualização do package.json com diretivas de resolução de dependências.

Pipeline e Cache: Invalidação dos artefatos locais (node_modules e lockfile) seguida de reconstrução limpa da árvore de pacotes.

Validação Contínua: Execução do build de produção (npm run build) validada para assegurar que não houve quebra na compilação do React ou na geração de estilos do Shadcn UI/Tailwind v4.

=== SPRINT 22 - 10/08/2026 ===
Resumo Técnico de Fechamento
1. Decisões de Arquitetura (BFF, Redux, EDA, Repositories)

Padrão de Repositório Estrito: Isolamento documentado e oficializado. Services orquestram regras de negócio; Repositories interagem com a camada de dados (Mongoose).

Validação de Entrada: Obrigatoriedade documentada da validação de DTOs via ZodValidationPipe antes da execução dos controllers.

Integridade e Persistência: Oficialização das regras inegociáveis de Soft Delete condicional (isActive: false) e uso de bibliotecas de precisão para manipulação de estoques e orçamentos.

2. Definições de Infraestrutura (Variáveis, Rotas, UI)

Controle de Acesso Multi-tenant: Roteamento B2B configurado e documentado para exigir contextos explícitos via headers (x-org-id, x-org-role).

Mapeamento de Domínios: Endpoints de gestão de Obras, Almoxarifado/Recursos, Storage e Planejamento integrados ao documento arquitetural.

Variáveis de Ambiente: Parametrização do .env atualizada com chaves de integração para MongoDB, Cloudflare R2 e SMTP (Resend).

=== SPRINT 21 - 05/08/2026 ===
Resumo Técnico de Fechamento
1. Decisões de Arquitetura

Segurança e Proteção (Account Lockout): Implementada proteção persistente contra credential stuffing e força bruta. A regra de negócio garante que o bloqueio sobreviva à volatilidade de instâncias serverless (Vercel), diferentemente de abordagens puramente in-memory.

Padrão de Repositório: Manutenção rigorosa do isolamento. As operações de incremento, bloqueio temporal e limpeza de contadores foram encapsuladas no AuthUsersRepository, mantendo o AuthService focado apenas na orquestração do fluxo de autorização.

Orquestração de Estado: Injeção de validações temporais (lockUntil) tanto na emissão inicial (signIn) quanto na renovação de credenciais (refreshToken), impedindo que sessões preexistentes contornem bloqueios recentes.

2. Definições de Infraestrutura

Modelagem de Dados: Evolução do UserSchema para suportar o estado transacional de segurança com a inclusão de loginAttempts (Number) e lockUntil (Date).

Mitigação Serverless: Adoção de persistência no MongoDB (Camada 2) para controle de taxa de longo prazo, mitigando o problema de cold starts e fragmentação de estado característicos de funções Lambda.

=== SPRINT 20 - 05/08/2026 ===
Resumo Técnico de Fechamento
1. Decisões de Arquitetura

Padrão de Repositório: Isolamento do acesso a dados concluído na camada de autenticação. O AuthUsersRepository assumiu a responsabilidade exclusiva pelas interações com o Mongoose, desacoplando o AuthUsersService.

Segurança e Validação de Entrada: Implementação do Zod para validação rigorosa dos payloads no controller (signInSchema), mitigando riscos de injeção de dados.

Ciclo de Vida do JWT: A renovação de tokens (Refresh Token) foi refatorada para realizar uma validação ativa no banco de dados. Privilégios (isSuperAdmin) e estado operacional (isActive) agora são validados em tempo real através do instanciamento estrito via new Types.ObjectId(id).

Padronização de Soft Delete: Inclusão estrutural da chave isActive no UserSchema com retrocompatibilidade para mitigar quebras em usuários legados.

2. Definições de Infraestrutura

Injeção de Dependências (DI): Refatoração do AuthModule para resolver vazamento de escopo. O contexto do Mongoose (MongooseModule.forFeature) e os provedores de repositório foram registrados exclusivamente onde são consumidos.

Observabilidade e Compliance: Remoção de logs não conformes que expunham o payload do JWT, mantendo o ambiente de produção seguro contra vazamento de PII.

Consistência de Dados: Emissão de script de atualização no nível do banco de dados (MongoDB) para normalização do estado isActive em documentos pré-existentes.

=== SPRINT 19 - 11/07/2026 ===

Resumo Técnico de Fechamento: Cobertura de Testes Unitários (ResourcesController)
1. Decisões de Arquitetura

Isolamento de Camadas (Mocking): Desacoplamento absoluto da camada de serviço (ResourcesService) durante a execução da suíte do controlador por meio da injeção de espiões (jest.fn()). A estratégia bloqueia qualquer transação de regra de negócio ou chamadas de I/O ao MongoDB.

Simulação de Contexto (Tenancy e RBAC): Aplicação do padrão Factory (mockRequest) para emulação determinística do objeto HTTP Request. A arquitetura garante a injeção estrita da identidade do usuário (req.user.sub) e do escopo de autorização (req.headers['x-org-role']) sem duplicação de código.

Testes de Comportamento e Barreira Lógica: Estruturação de asserções negativas (rejects.toThrow) para atestar as travas físicas de Controle de Acesso Baseado em Funções (RBAC) do controlador, assegurando o disparo de ForbiddenException para acessos indevidos.

Neutralização Perimetral: Utilização da diretiva overrideGuard para bypass do AuthGuard, isolando a métrica de teste exclusivamente para as regras de roteamento e extração de parâmetros.

2. Definições de Infraestrutura

Resolução de Conflitos de Tipagem: Expansão da árvore de dependências de desenvolvimento do back-end (via pnpm) com a injeção do pacote @types/jest, solucionando falsos-positivos do compilador TypeScript (tsc) em ambientes isolados.

Expurgo de Módulos Concorrentes: Remoção da biblioteca nativa node:test para forçar o direcionamento integral das asserções e rotinas de ciclo de vida (describe, it, beforeEach) ao motor de testes estabelecido no NestJS (Jest).

Estratégia de Regressão Estrutural: Definição procedimental explícita que dispensa o uso de arquivos de Snapshot no escopo de controladores REST, priorizando a avaliação contínua dos contratos de delegação.

=== SPRINT 18 - 06/07/2026 ===
=== Resumo Técnico de Fechamento: Melhoria de Ergonomia em Prompts (Módulo Summary) ===

**1. Decisões de Arquitetura (Estado e Validação)**

* **Preservação de Hidratação Segura:** A arquitetura de persistência local (`cazua_reel_form_draft`) via Web Storage API e a montagem protegida por `useEffect` (`isMounted`) foram rigorosamente mantidas. Isso garante o funcionamento fluido no Next.js (App Router) e previne erros de *Hydration Mismatch* no Server-Side Rendering (SSR).
* **Consistência de Contrato (Zod):** Nenhuma mutação foi feita no esquema de validação (`createReelSchema`). A tipagem e o contrato com o BFF permanecem inalterados e confiáveis.

**2. Definições de Infraestrutura (UI/UX e Design B2B)**

* **Refatoração de Inputs:** Modificação estrutural no componente `ReelGenerator.tsx`. Os campos curtos (`input`) foram substituídos por áreas de texto (`textarea`) configuradas com `min-h-[80px]` e `resize-y`. Essa alteração acomoda a volumetria natural de prompts textuais para IA sem truncar a visualização.
* **Geometria e Tokens Semânticos:** Aplicação mantida das regras B2B. As textareas respeitam o *border-radius* estrito (`rounded-md` / 4px) e consomem tokens do Tailwind nativo (`bg-background`, `border-border`, `focus:ring-ring`), garantindo suporte orgânico ao Dark Mode sem a injeção de cores absolutas. O campo `duracao` foi preservado como input, dado seu escopo numérico restrito.

Defina o objetivo técnico ou funcional para iniciarmos o próximo Sprint.

=== SPRINT 17 - 05/07/2026 ===
=== Resumo Técnico de Fechamento: Módulo de Geração de Conteúdo (Reels IA) ===

**1. Decisões de Arquitetura (BFF, Validação e Gerenciamento de Estado)**

* **Padrão BFF (Backend-For-Frontend):** Implementação de Route Handlers proxy (`/api/summary/reels/generate` e `/api/summary/text`) no Next.js. A camada de borda agora atua como escudo, injetando de forma transparente os tokens de autorização e o contexto de Multi-tenancy (`x-org-id`, `x-org-role`).
* **Contratos Simétricos (Zod):** Estabelecimento do `createRellSchema` operando simultaneamente na validação do formulário (Client-side via `zodResolver`) e na sanitização de borda (BFF), impedindo tráfego malformado antes de atingir o micro-serviço.
* **Gerenciamento de Estado de UI:** Utilização da Web Storage API (`localStorage`) para persistência transiente (draft) do formulário e do retorno da IA. A estratégia preserva a árvore global do Redux imaculada, focando apenas em dados de domínio consolidados.
* **Persistência Postergada:** O armazenamento em banco de dados foi desvinculado da geração. O NestJS atua como ponte transacional para a OpenAI, delegando a gravação final para um fluxo de aprovação manual do usuário.
* **Interceptação de Rede:** Adaptação do `summaryService.ts` para forçar o roteamento via `axiosInstance` local (`baseURL: ''`), garantindo que os interceptadores do Redux apliquem a autenticação sem quebrar o fluxo.

**2. Definições de Infraestrutura (Rotas, Controladores e UI)**

* **Roteamento Back-end (NestJS):** Correção de colisão de rotas no `SummaryController`. Segregação semântica entre os endpoints de sumarização (`POST /summary/text`) e geração de roteiros (`POST /summary/reels/generate`).
* **Composição de UI (ReelGenerator.tsx):** Adoção de grid CSS estrutural de duas colunas (Setup de IA vs Edição de Rascunho).
* **Design Tokens e Geometria B2B:** Imposição do mapeamento de variáveis semânticas do Tailwind v4 (`bg-card`, `bg-background`, `border-border`, `text-foreground`) assegurando suporte nativo a Dark Mode. Raio de borda rigidamente travado em 4px (`rounded-md`) e iconografia `lucide-react`.
* **Feedback de Ação:** Integração do sistema de toast (`sonner`) para notificar os estados da transação de rede (geração, erro, limpeza de cache).

Como próximo passo, dimensione a arquitetura de persistência: inicie o desenho do esquema no Mongoose (`ReelSchema` ou `ContentSchema`) e a rota correspondente de `POST /summary/reels/save` no NestJS, implementando o Padrão de Repositório para armazenar o rascunho aprovado. Defina quando deseja iniciar esta etapa.

=== SPRINT 16 - 30/06/2026 ===
====================================================================
ATUALIZAÇÃO DE ARQUITETURA: BFF, Segurança e Resiliência (Next.js 15)
====================================================================

1. Centralização do Backend-For-Frontend (BFF):
   - Criado o utilitário padrão `src/lib/api/serverUtils.ts` para as Serverless Functions do Next.js.
   - Aplicação do princípio DRY (Don't Repeat Yourself) para injetar configurações de rede e segurança em todas as rotas `/api/...`, substituindo código duplicado nas chamadas de fetch interno.

2. Resolução de Roteamento Dinâmico (Vercel vs Docker):
   - O Next.js foi blindado contra o "Build-Time Inlining" de variáveis de ambiente.
   - Implementada a função estrita `getNestApiUrl()`, que utiliza validação por `NODE_ENV === 'production'` para ignorar variáveis de container (ex: `http://backend:3001`) quando o deploy ocorre na Vercel, forçando o uso da `NEXT_PUBLIC_API_BASE_URL`.

3. Estratégia de Transição de Segurança (Strangler Fig Pattern):
   - O BFF agora implementa Resolução Híbrida de Tokens (`getBffAuthHeader`). 
   - Ele aceita tanto o padrão legado (Token trafegado via `Authorization` Header injetado pelo Redux) quanto o novo padrão alvo (Cookies `HttpOnly`). Isso permite migrar módulos um a um para alta segurança (contra ataques XSS) sem quebrar o sistema atual.

4. Propagação de Contexto (Multi-tenancy):
   - As informações do Tenant (`x-org-id` e `x-org-role`) são lidas de forma nativa pelo utilitário `getBffOrgHeaders` no Server-Side e repassadas de forma invisível para o NestJS, garantindo que o `TenantGuard` atue perfeitamente.

5. Compatibilidade Next.js 15:
   - Todas as rotas dinâmicas do BFF (ex: `[id]/route.ts`) foram atualizadas para tratar `params` como uma Promise (ex: `const { id } = await context.params`), cumprindo a nova assinatura da framework e evitando falhas de compilação em produção.
====================================================================


=== SPRINT 15 - 27/06/2026 ===
=== Resumo Técnico de Fechamento: Unificação de Busca Textual no Módulo de Posts ===

1. Decisões de Arquitetura (BFF, Redux, EDA, Repositories)

Repositórios (Mongoose): Alteração no contrato de paginação (PaginateOptions) para injetar o parâmetro opcional term. Aplicação de cláusula $or com expressão regular (Regex i) no filtro principal dos métodos getAllPosts e getTotalPostsCount. Isso garante a integridade matemática da paginação sobre conjuntos de dados filtrados.

Expurgo de Código Morto: Remoção estrutural do método isolado searchPost no contrato abstrato e na implementação do Mongoose, centralizando a responsabilidade de leitura no método principal de listagem.

Contrato Front-end (Redux): Preservação absoluta da barreira BFF. Atualização do slice de estado (fetchPosts Thunk) e do postService.ts para serializar o termo dinamicamente via URLSearchParams, garantindo propagação previsível da requisição até a borda (Next.js Route Handlers).

2. Definições de Infraestrutura (Variáveis, Rotas, UI)

Roteamento Backend (NestJS): Unificação de endpoint. A rota legada @Get('search') foi depreciada e removida. A busca ocorre nativamente na rota paginada primária (GET /posts), absorvendo o parâmetro via @Query('term').

Proteção de Infraestrutura (Front-end): Implementação de Debounce lógico (500ms) no controle de estado transiente do input de busca. A interface aguarda a inércia da digitação antes de acionar a store do Redux, mitigando gargalos de I/O de rede e chamadas redundantes ao banco de dados.

Geometria e Design B2B: Refatoração do cabeçalho da UI (PostList.tsx). O input de busca respeita os tokens semânticos de alto contraste (bg-background, text-foreground, border-border, focus em ring-ring). A geometria mantém-se travada no padrão da marca (rounded-md, equivalente a 4px), utilizando exclusivamente o vetor Search da biblioteca lucide-react.

=== SPRINT 14 - 24/06/2026 ===
=== SPRINT DE CORREÇÕES (HOTFIX) FINALIZADA ===
=== Resumo Técnico de Fechamento Complementar: Redes Docker, Contratos BFF e RBAC na UI ===

Adicione o complemento abaixo ao documento-arquitetura-vivo.txt para registrar o débito técnico resolvido.

1. Decisões de Arquitetura (BFF, Redux, RBAC)

Adaptação de Contrato (BFF): Implementada sanitização de payload no Next.js Route Handlers. O vazamento da chave legada _id do MongoDB foi interceptado na borda e transmutado para id, garantindo o respeito ao contrato estrito (IPost) esperado pela UI sem onerar o Client-Side.

Manejo de Erros no Redux: Adoção do método .unwrap() nos Thunks de mutação (ex: createNewPost). Erros emitidos pelo BFF (400, 401, 500) agora estouram o bloco catch do componente, permitindo que a interface retenha o usuário no formulário e exiba feedbacks destrutivos contextualizados.

Role-Based Access Control (RBAC) na UI: Injeção de estado híbrido de autorização (user.role e currentOrg.role) nos componentes de listagem. Botões de mutação (Criar, Editar, Deletar) e Empty States acionáveis foram blindados na interface, visíveis estritamente para perfis ADMIN ou OWNER.

2. Definições de Infraestrutura (Redes e Variáveis de Ambiente)

Segregação de DNS (Server-to-Server vs Client-to-Server): Estabelecimento do padrão arquitetural de conectividade para ambientes orquestrados (Docker/Vercel).

NEXT_PUBLIC_API_BASE_URL: Expõe a rota para o navegador (Client-Side) realizar fetches legados.

INTERNAL_API_URL: Variável sigilosa (Server-Side) utilizada estritamente pelo BFF para resolução de DNS interno na rede Docker (http://backend:3001), evitando roteamento desnecessário pela internet pública e eliminando o erro de loopback (ECONNREFUSED no localhost). O sistema possui fallback nativo para operar sem intervenções na Vercel.

O módulo Posts está homologado, blindado e aderente à arquitetura de referência.


=== SPRINT 13 - 23/06/2026 ===
O objetivo desta Sprint foi finalizado com sucesso. Abaixo está a documentação oficial para integrar ao `documento-arquitetura-vivo.txt`.

=== SPRINT ATUAL ===
=== Resumo Técnico de Fechamento: Refatoração do Módulo Posts, Implementação de BFF e Higienização de UI ===

**1. Decisões de Arquitetura (BFF, Redux, Validação, Repositories)**

* **Padrão BFF (Backend-For-Frontend):** Estabelecido proxy reverso seguro no Next.js (`Server-Side Route Handlers`). O front-end agora desconhece a URL do micro-serviço e não manipula tokens de autenticação diretamente, delegando a injeção ao servidor de borda via cookies estritos.
* **Contratos Simétricos (Zod):** Implementação da fonte única de verdade (`post.zod.ts`). O BFF valida e rejeita lixo na borda (evitando tráfego inútil), e o NestJS barra requisições externas não autorizadas.
* **Gerenciamento de Estado (Redux):** Desacoplamento do micro-serviço. O `postsSlice` agora consome estritamente o serviço local (`/api/posts`). Higienização total de vazamento de infraestrutura do Mongoose (substituição massiva de `_id` por `id`).
* **Isolamento de Banco de Dados:** Correção do `PostMongooseRepository`. Aplicação estrita de `new Types.ObjectId(id)` e mitigação de bugs lógicos (`organizationId !== 'undefined'`), garantindo que a camada de Service continue agnóstica ao MongoDB.

**2. Definições de Infraestrutura (Variáveis, Rotas, UI)**

* **Roteamento (Next.js App Router):** Implementação padronizada no diretório correto de rotas de API (`src/app/api/posts/...`) utilizando suporte assíncrono moderno (`await cookies()`).
* **UI/UX e Design Tokens:** Refatoração do `PostForm.tsx`. Remoção absoluta de cores estáticas. Imposição do padrão B2B Minimalista com variáveis semânticas do Tailwind v4 (`bg-background`, `bg-card`, `text-foreground`, `border-border`), garantindo suporte nativo a Dark Mode.
* **Geometria:** Border-radius padronizado em 4px (`rounded-md`) e adoção exclusiva da biblioteca `lucide-react` para iconografia.



=== SPRINT 12 - 12/06/2026 ===
=== Resumo Técnico de Fechamento: Refatoração do Emissor de Pareceres e Simulação A4 ===

**Decisões de Arquitetura:**

* **Persistência Integral de Rascunho Local:** Alteração do schema de cache do `localStorage` (chave `draft_parecer_{id}`). O dado salvo transitou de uma string plana para um objeto JSON estruturado `{ text, attachments }`. Isso garante a amarração temporal entre o texto em andamento e as URLs dos arquivos já persistidos no Cloudflare R2, eliminando o gargalo de anexos órfãos após o desmonte (unmount) acidental do componente.
* **Fonte de Verdade de UI (Single Source of Truth):** Extração das options do select de status para a constante `STATUS_OPTIONS` acoplada ao type `ProjectStatus`. Isso unifica a regra de negócio visual e engloba nativamente o novo status `ON_HOLD` sem necessidade de manipulação do DOM.

**Definições de Infraestrutura:**

* **Motor WYSIWYG Nativo (CSS Grid):** Substituição de recálculos dispendiosos de JavaScript (`scrollHeight` no *event loop* do React) por uma arquitetura responsiva baseada em CSS Grid. O emprego de um *ghost element* invisível força fisicamente o container da "Folha A4" a expandir em exata sincronia com a volumetria do texto digitado no `<textarea>`.
* **Quebra de Página Infinita (Tiling):** Adoção de `repeating-linear-gradient` com espaçamento milimétrico fixo (`297mm`) renderizado em GPU pelo navegador, fornecendo limitadores visuais de página consistentes com a biblioteca de exportação PDF, independente do crescimento vertical do modal e preservando os 60 FPS da UI.
* **Refatoração do Bloco de Assinatura:** Remoção de âncoras absolutas propensas a quebra de Z-index e vazamento de texto. O bloco de assinatura foi realocado para o fluxo natural do DOM (block flow), sendo automaticamente deslocado para baixo à medida que o texto preenche as páginas virtuais simuladas.


=== SPRINT 11 - 11/06/2026 ===
### Resumo Técnico de Fechamento: Implementação de Aba "Paradas" e Preferências de Visibilidade

**Decisões de Arquitetura:**

* **Expansão de Domínio:** Inserção do status `ON_HOLD` no enum `ProjectStatus` (`project.schema.ts`) para representar o ciclo de vida de obras paralisadas ou suspensas.
* **Validação (BFF):** Atualização dos contratos Zod (`createProjectSchema`, `emitParecerSchema`, `bulkImportItemSchema`) em `projects.zod.ts` para suportar o tráfego seguro do novo status.
* **Tipagem Front-end:** Extensão das interfaces em `src/types/project.ts` (`ProjectStatus` e `TabType`), garantindo consistência estrita de ponta a ponta.
* **Gerenciamento de Estado UI:** Adoção da Web Storage API (`localStorage` via chave `cazua_tab_preferences`) para persistir preferências do usuário de forma assíncrona, não poluindo a store global do Redux com metadados visuais.
* **Separação de Responsabilidades:** Desacoplamento da interface de configuração através do componente isolado `TabPreferencesModal.tsx`.

**Definições de Infraestrutura:**

* **Adaptação para Dark Mode:** Refatoração massiva em `ProjectsList.tsx` e `TabPreferencesModal.tsx`, substituindo cores absolutas por design tokens nativos (`bg-background`, `bg-card`, `text-foreground`, `border-border`) integrados ao Tailwind CSS e Shadcn UI.
* **Identidade Visual B2B (Status `ON_HOLD`):** Definição de paleta baseada em tons `stone` escuros (`bg-stone-800 dark:bg-stone-700`) e iconografia semântica (`PauseCircle` do `lucide-react`) para diferenciar o status sem acionar gatilhos visuais de erro (reservados para o vermelho crítico).
* **Prevenção de Falhas de UI:** Implementação de lógicas de *fallback* e bloqueios no `ProjectsList.tsx` para impedir a deleção de todas as abas e autoajustar a aba ativa caso a atual seja ocultada.

Defina o objetivo técnico ou funcional para iniciarmos o próximo Sprint.


=== SPRINT 10 - 11/06/2026 ===
=== Resumo Técnico de Fechamento: Preferências de Visibilidade de Abas e Adaptação para Dark Mode ===

Decisões de Arquitetura

Gerenciamento de Estado Local: Adoção da Web Storage API (localStorage com a chave cazua_tab_preferences) para persistência das configurações de visibilidade das abas. A estratégia evita overhead de rede na API (BFF) e mantém a árvore de estado global (Redux) estrita aos dados de negócio.

Separação de Responsabilidades (UI): Desacoplamento da interface de configuração através da criação do TabPreferencesModal.tsx, isolando a lógica de filtragem e estado transitório de seleção do componente principal ProjectsList.tsx.

Suporte Nativo a Temas (Dark Mode): Remoção completa de valores hexadecimais e utilitários de cor estáticos (bg-white, bg-[#8B4513], text-stone-900) no nível de componente. Adoção obrigatória das variáveis semânticas do CSS/Shadcn UI (bg-card, bg-primary, text-foreground, border-border), garantindo transição automática entre modo claro e escuro.

Definições de Infraestrutura

Renderização Dinâmica de Navegação: Modificação no motor de renderização do ProjectsList.tsx para aplicar um filtro includes iterativo sobre o array allTabsConfig com base nas preferências recuperadas em tempo de montagem (useEffect).

Tratamento de Conflitos de Estado: Implementação de proteções lógicas para evitar que a interface quebre caso o usuário oculte a aba atualmente ativa (forçando fallback para a primeira aba disponível) e bloqueio mecânico que impede a deleção da última aba visível.

Sincronização de URL (Deep Linking): Inserção de regra que força a reexibição imediata de uma aba caso ela seja acessada diretamente via parâmetro de URL (?tab=...), sobrepondo as preferências locais.

Geometria B2B: Aplicação estrita do token de design de borda da marca (rounded-md, equivalente ao --radius: 0.25rem base) em todo o modal, com iconografia restrita ao lucide-react (SlidersHorizontal).


=== SPRINT 09 - 06/06/2026 ===
=== RESUMO TÉCNICO DE FECHAMENTO ===

Funcionalidade: Atualização Massiva de Custos (Módulo de Planejamento)

Decisões de Arquitetura:

Otimização de Banco de Dados: Implementação do método updateCostsFromExcel no PlanningService utilizando operações bulkWrite (com updateMany) do Mongoose. Em vez de deletar e recriar, o sistema agora injeta os novos valores na chave custo buscando pelo índice relacional (codigoComposicao + metadados de lote), garantindo performance e integridade dos nós e sub-nós (resumos e insumos).

Validação de Entrada: Reaproveitamento do uploadPlanningSchema (Zod) no BFF, blindando a mutação para que atue exclusivamente no escopo temporal/geográfico especificado (state, referenceMonth, referenceYear, isGlobal).

Gerenciamento de Estado: Criação da action assíncrona uploadCostsFile (Redux Thunk) dedicada à atualização, separando as responsabilidades transacionais de criação (upload de base) e mutação (atualização de custo). Tratamento local do fluxo de UI via .unwrap() para garantir feedback determinístico (sucesso/erro).

Parsing Restrito: Validação conceitual do parser (xlsx), impondo a necessidade estrita de planilhas tabuladas (duas colunas reais) para o mapeamento seguro da injeção no backend.

Definições de Infraestrutura:

Roteamento Backend: Exposição do novo endpoint POST /planning/upload-costs no PlanningController, mantendo a barreira de segurança via JWT e checagem da variável de ambiente SUPER_ADMIN_EMAIL.

Roteamento Frontend: Adição do respectivo construtor multipart/form-data uploadCostsFromExcel na camada planningService.

Refatoração UI/UX Global (Sistema Cazuá): Criação do UploadCostsModal.tsx e atualização da página de planejamento aplicando as tokens dinâmicas (bg-background, bg-card, text-foreground, border-border). Isso elimina classes estáticas e garante 100% de compatibilidade com o Dark Mode e o padrão cromático sóbrio em escala stone.


=== SPRINT 08 - 02/06/2026 ===
Resumo Técnico de Fechamento: Consolidação de Diretrizes de Agente (System Instructions)

Decisões de Arquitetura:

Unificação do prompt de sistema (System Instructions) para controle de qualidade na geração de código Front-end e Back-end.

Bloqueio explícito de escalas de cor concorrentes (gray, slate, zinc), impondo o uso restrito do design token stone para preservar a identidade visual B2B da marca Cazuá.

Definição de geometria técnica rigorosa com border-radius base de 4px (--radius: 0.25rem), garantindo consistência estrita em botões, cards e modais gerados via Shadcn UI e Tailwind v4.

Reforço da stack arquitetural orientada a eventos (NestJS + EventEmitter2) e imposição da separação arquitetural de responsabilidades (Services vs Repositories).

Definições de Infraestrutura:

Atualização da base comportamental do agente finalizada. O modelo atuará doravante sob as novas restrições visuais e procedimentais.

=== SPRINT 07 - 31/05/2026 ===
Resumo Técnico de Fechamento: Módulo de Planejamento com Upload Excel e Busca Dinâmica

Decisões de Arquitetura

* **Estrutura de Dados (Schema):** Implementação do schema `Planning` no MongoDB consolidando dados de composições de obra (SINAPI e customizadas por organização) com campos: `isGlobal` (booleano), `organizationId` (string nullable), `state` (UF), `referenceMonth`/`referenceYear` (versionamento temporal), `codigoComposicao` (agrupador), `tipo` (COMPOSICAO | INSUMO | vazio), `insumo` (identificador), `descricao`, `unidade`, `coeficiente`, `custo` (COM CUSTO | COM PREÇO), `isSummary` (booleano para distinção resumo vs. detalhe).

* **Indexação para Performance:** Criação de índices compostos (`state`, `referenceMonth`, `referenceYear`, `grupo`, `codigoComposicao`, `isSummary`) e índice full-text nas colunas textuais (`descricao`, `grupo`, `tipo`, `insumo`, `codigoComposicao`, `custo`) para suportar buscas relâmpago em grandes volumes de dados.

* **Parser Excel (Serviço):** Integração da biblioteca `xlsx@^0.18.5` no método `uploadFromExcel()` do `PlanningService` com normalização de colunas (remoção de acentos, lowercase) e tratamento de tipagem heterogênea (números com vírgula, strings vazias).

* **Validação e Tipagem (BFF):** Schemas Zod (`uploadPlanningSchema`, `searchPlanningSchema`) com pré-processadores customizados (`parseBoolean`, `parseNullableString`, `parseNumber`) para conversão de strings e tratamento de valores ausentes antes de alcançar a camada de serviço.

* **Operações de Consulta (Repositório):** Implementação de três padrões de busca no `PlanningService`:
  - `search()`: Paginação com filtros compostos e busca textual via operador `$text`.
  - `findCompositionItems()`: Recuperação de todos os itens agrupados por `codigoComposicao`, ordenados (sumário primeiro, depois detalhes).
  - `grouped()`: Agregação dinâmica via pipeline MongoDB com agrupamento customizável por campos (`state`, `referenceYear`, `referenceMonth`, `grupo`), sem forçar estrutura de armazenamento.

* **Reuso de Dados:** Metadados `isGlobal`, `organizationId`, `state`, `referenceMonth`, `referenceYear` propagados em todas as operações (upload, busca, agrupamento) para suportar tanto base SINAPI global quanto composições customizadas por empresa.

Definições de Infraestrutura

* **Roteamento (API NestJS):**
  - `POST /planning/upload`: Upload de arquivo `.xlsx` com metadados, retorna `{ insertedCount, metadata }`.
  - `GET /planning/search`: Busca paginada com filtros (`q`, `isGlobal`, `state`, `referenceMonth`, `referenceYear`, `grupo`, `codigoComposicao`, `tipo`, `insumo`, `summaryOnly`).
  - `GET /planning/grouped`: Retorna agregação dinâmica agrupada por campos customizáveis (`groupBy` em CSV).
  - `GET /planning/composition/:codigoComposicao/items`: Detalhe completo de uma composição (sumário + todos os insumos).

* **Interceptor de Upload:** Utilização de `FileInterceptor('file')` via `@nestjs/platform-express` com armazenamento em memória (`memoryStorage()`) para evitar I/O de disco e reduzir latência do parser.

* **Dependências Adicionadas:** `xlsx@^0.18.5` (parsing Excel), `multer@^1.4.5-lts.1` (multipart form handling). Ambas adicionadas ao `BackEnd/package.json` e instaladas via `pnpm install`.

* **Registração no Módulo:** `PlanningModule` importado no `AppModule` com `MongooseModule.forFeature([{ name: Planning.name, schema: PlanningSchema }])`.

* **Testes Unitários:** Cobertura com 10 testes passando (5 no `PlanningService`, 5 no `PlanningController`) cobrindo upload, busca, agrupamento e detalhe de composição.

* **Validação Docker:** Container backend rodando sem erros de módulo (`[InstanceLoader] PlanningModule dependencies initialized`), todos os 4 endpoints mapeados e aplicação iniciada com sucesso (`Nest application successfully started`).

Detalhe de Comportamento

- **Composição e Resumo:** Quando `tipo === 'COMPOSICAO'` ou `tipo === ''` ou `insumo` está vazio, `isSummary = true`. Resumos são sempre os primeiros na ordenação (índice descrito).
- **Versionamento:** Dados SINAPI mantêm `isGlobal: true` com `referenceMonth/Year` fixos. Dados customizados pela empresa têm `isGlobal: false`, `organizationId` preenchido, e podem ignorar `referenceMonth/Year` se preferirem gerenciar "vivo" sem histórico.
- **Agrupamento Flexível:** Frontend pode solicitar agrupamento por qualquer combinação de campos (ex.: `?groupBy=state,referenceYear,grupo`), retornando um resumo agregado sem refatorar a estrutura de persistência.

=== SPRINT 06 - 25/05/2026 ===
Resumo Técnico de Fechamento: Fluxo de Devolução de Recursos (Obra para Almoxarifado)

Decisões de Arquitetura

* Lógica Transacional (Back-end): Efetivação do método `returnFromProject` no `ResourcesService`, responsável por registrar a transação (`RETURN`), atualizar o saldo no Almoxarifado (`currentStock`) e abater o valor do extrato consolidado da obra.


* Emissão de Eventos: Disparo de evento `timeline.create` via `EventEmitter2` para registro automático da devolução na Timeline da obra.


* Gerenciamento de Estado Global (Front-end): Implementação do `returnResourceThunk` no `resourcesSlice.ts` via Redux Toolkit para gerenciar o ciclo de vida da requisição e abstrair o tratamento de erros.


* Validação e Tipagem Estrita: Utilização do `returnResourceSchema` via Zod integrado ao `react-hook-form` no Front-end, garantindo que o payload (`resourceId`, `quantity`, `origin`) obedeça ao contrato da API antes da submissão.


* Reuso de Dados Consolidados: Injeção do objeto `statement.items` do Redux diretamente no modal de devolução (`ReturnResourceModal.tsx`), garantindo que apenas recursos com saldo alocado na obra sejam apresentados para estorno/devolução.



Definições de Infraestrutura

* Roteamento (API NestJS): Rota `POST /organizations/:orgId/resources/return/:projectId` consolidada e protegida por `AuthGuard`.


* Serviço de Rede (Next.js): Adição do método `returnFromProject` no `resourceService.ts` utilizando a instância configurada do Axios.


* Interface de Usuário (UI): Refatoração da tabela em `ProjectResourcesTab.tsx` para exibição integral das justificativas (remoção do truncamento) e integração do novo fluxo de modal de devolução.


* Dependências: Adição dos pacotes `zod`, `@hookform/resolvers` e `react-hook-form` ao escopo do workspace do Front-end.

=== SPRINT 05 - 25/05/2026 ===
### Resumo Técnico de Fechamento: Sanitização de Precisão Decimal

**Decisões de Arquitetura**

* **Utilitário de Domínio:** Criação do módulo `Precision` (`src/shared/utils/precision.util.ts`) implementando mitigação matemática via `Number.EPSILON` para contornar falhas de ponto flutuante nativas da V8 Engine.
* **Validação de Borda (BFF):** Refatoração do `resource.zod.ts` com injeção do encadeamento `.transform((val) => Precision.round(val, 2))` nos schemas de entrada (`createResourceSchema`, `allocateResourceSchema`, `addStockSchema`, `returnResourceSchema`), garantindo higienização de quantitativos e custos antes da camada de serviço.
* **Motor de Serviço:** Integração do utilitário `Precision.round` no `ResourcesService` para o cálculo exato de `finalTotalCost` nas lógicas transacionais (alocação direta, aprovação de RMs, entrada e devolução de estoque).
* **Expurgo de Passivo:** Criação do método `sanitizeDecimals` no serviço, restrito a cargos de governança (`OWNER`, `ADMIN`), para invocação de rotina de limpeza de dados legados corrompidos.

**Definições de Infraestrutura**

* **Persistência (Repositórios):** Atualização do `ResourceRepository` e `ResourceTransactionRepository` com a função de sanitização em lote utilizando `updateMany` e operações de pipeline (`[{ $set: ... }]`).
* **Pipeline de Agregação:** Alteração do método `getProjectCostStatement` no MongoDB. Inserção de estágios de `$project` contendo o operador `$round` após os blocos `$group`, interceptando e neutralizando dízimas binárias (IEEE 754) residuais geradas durante as somatórias (`$sum`) dos extratos financeiros por insumo e por categoria.

=== SPRINT 04 - 24/05/2026 ===
### 📄 Resumo Técnico do Sprint: Infraestrutura de Comunicação, CI/CD e Tipagem

**Objetivo Principal:** Implementar o fluxo de recuperação de senhas via e-mail transacional e estabilizar a esteira de *deploy* contínuo (CI/CD) na Vercel e orquestração Docker local.

#### 1. Infraestrutura Local e Orquestração (Docker + PNPM)

* **Desafio:** Contêineres quebrando na etapa de instalação devido a *breaking changes* na atualização silenciosa para o `pnpm v10` e bloqueios de segurança em scripts de compilação nativa. Falhas no mapeamento do `.env` e travamento da rede WSL (`Vsock`).
* **Solução:** * Fixação (*downgrade* travado) do motor de dependências para o `pnpm@9` nos `Dockerfile.dev`.
* Implementação da configuração moderna de monorepo criando o arquivo `pnpm-workspace.yaml`, mapeando os pacotes `frontend` e `backend`.
* Liberação centralizada de dependências nativas (`@tailwindcss/oxide`, `core-js`, `sharp`) através da diretiva `allowBuilds`.
* Correção do apontamento do arquivo `.env` através da instrução `env_file` no `docker-compose.yml`.



#### 2. Mensageria e Comunicação (SMTP via Resend)

* **Desafio:** Garantir a entregabilidade (evitar a caixa de spam), contornar a interceptação de TLS no ambiente local e adaptar o envio transacional para o ambiente *Serverless* da Vercel.
* **Solução:**
* Configuração e autoridade de domínio estabelecida via Cloudflare (registros DNS: SPF, DKIM, DMARC e MX).
* Integração do `nodemailer` mantendo o código agnóstico de provedor (sem *vendor lock-in*).
* Adição de `tls: { rejectUnauthorized: false }` no transporter para permitir envios a partir de redes Docker/WSL locais.
* Resolução do erro `501 Bad sender address syntax` em produção, limpando os caracteres especiais da variável `MAIL_FROM` na Vercel e formatando o cabeçalho dinamicamente no código (`Sistema Cazuá <${process.env.MAIL_FROM}>`).



#### 3. CI/CD e Tipagem Estrita (Vercel & TypeScript)

* **Desafio:** A Vercel (ambiente de produção) utilizou o compilador estrito do TypeScript (`tsc`), derrubando o *build* devido a falhas na manipulação de IDs do Mongoose e conflitos entre a tipagem do Node.js e a Fetch API nativa.
* **Solução:**
* **Mongoose:** Refatoração massiva em repositórios, *guards* e serviços para instanciar corretamente os ObjectIds usando `new Types.ObjectId(id as string)`. Padronização da validação através de `Types.ObjectId.isValid()`.
* **Express vs. Fetch API:** Aplicação de um *bypass* cirúrgico (utilizando o tipo estático `any`) nos objetos `Request` e `Response` dentro do `AuthGuard` e do `HttpExceptionFilter`. Isso silenciou falsos-positivos de compilação relacionados às propriedades `.headers`, `.status` e `.url`, forçando a aprovação do *deploy*.



#### 4. Governança de Variáveis de Ambiente

* **Definição Arquitetural:** Estabelecemos a separação de responsabilidades. O Front-end (Vercel) guarda a `NEXT_PUBLIC_API_BASE_URL` para encontrar a API. O Back-end (também na Vercel) guarda a `FRONTEND_URL` e as credenciais seguras do provedor SMTP (`SMTP_PASS`, etc.) injetadas em tempo de *build* nas Serverless Functions.

---

Com a esteira de deploy operando perfeitamente e a fundação de e-mails transacionais em produção, o sistema ganhou autonomia de comunicação e infraestrutura escalável.


=== SPRINT 01 - 07/05/2026 ===
Resumo Técnico de Fechamento: Interface Comercial e Technical SEO

Decisões de Arquitetura:

Refatoração da camada de estilos (FrontEnd/src/app/globals.css) para implementação de Design Tokens baseados nas paletas Cazuá e Stone, visando conformidade de contraste WCAG AA.

Modularização da Landings Page em componentes focados na proposta de valor B2B (HeroSection.tsx, FeatureSection.tsx, PricingSection.tsx) e estruturação visual dos níveis de assinatura (FREE, PRO, ENTERPRISE).

Atualização do copy e layout da página de autenticação (FrontEnd/src/app/(auth)/login/page.tsx) com eliminação de jargões técnicos na comunicação externa.

Implementação da API nativa de Metadata do Next.js (App Router) no layout.tsx (escopo global e Open Graph) e page.tsx (escopo de conversão).

Definições de Infraestrutura:

Geração dinâmica de sitemap XML (FrontEnd/src/app/sitemap.ts) priorizando a indexação de rotas públicas.

Configuração de diretivas de rastreamento (FrontEnd/src/app/robots.ts) com bloqueio explícito de acesso dos motores de busca aos diretórios restritos (/dashboard/, /api/).

Exigência de configuração da variável de ambiente NEXT_PUBLIC_APP_URL no ambiente de produção para a montagem correta das URLs nos arquivos de SEO

=== SPRINT 02 - 09/05/2026 ===
Resumo Técnico de Fechamento: Extrato de Custos da Obra

Decisões de Arquitetura

Motor de Cálculo (Repositório): Implementação de Aggregation Pipeline no resource-transaction.repository.ts utilizando o operador $facet. A query consolida transações do tipo ALLOCATION e RETURN (invertendo o sinal da devolução) em uma única ida ao banco, gerando duas ramificações: resumo por categoria (ResourceType) e resumo por insumo detalhado.

Validação e Tipagem (BFF): Atualização do projectStatementSchema no Zod (resource.zod.ts) para tipagem estrita da resposta contendo totalAccumulated, categories e items.

Gerenciamento de Estado (Front-end): Criação do módulo resourcesSlice.ts no Redux Toolkit. A chamada de rede foi abstraída via createAsyncThunk (fetchProjectStatement), armazenando o estado consolidado (statement, loading, error) no reducer global (store.ts).

Separação de Responsabilidades (UI):

Criação da aba ProjectCostTab.tsx dedicada exclusivamente à visualização analítica (Custo Total, Composição por Categoria e Tabela de Insumos Consolidados).

Refatoração da aba ProjectResourcesTab.tsx para operar estritamente como Livro Razão transacional (fluxo de aprovação/estorno).

Definições de Infraestrutura

Roteamento (API NestJS): Novo endpoint exposto GET /organizations/:orgId/resources/statement/:projectId via resources.controller.ts, protegido por AuthGuard.

Serviço de Rede (Next.js): Atualização do axiosInstance em resourceService.ts com o novo método getProjectStatement injetando implicitamente as credenciais da organização.

=== SPRINT 03 - 11/05/2026 ===
Resumo Técnico de Fechamento: Extrato de Custos da Obra

Decisões de Arquitetura

Motor de Cálculo (Repositório): Implementação de Aggregation Pipeline no resource-transaction.repository.ts utilizando o operador $facet. A query consolida transações do tipo ALLOCATION e RETURN (invertendo o sinal da devolução) em uma única ida ao banco, gerando duas ramificações: resumo por categoria (ResourceType) e resumo por insumo detalhado.

Validação e Tipagem (BFF): Atualização do projectStatementSchema no Zod (resource.zod.ts) para tipagem estrita da resposta contendo totalAccumulated, categories e items.

Gerenciamento de Estado (Front-end): Criação do módulo resourcesSlice.ts no Redux Toolkit. A chamada de rede foi abstraída via createAsyncThunk (fetchProjectStatement), armazenando o estado consolidado (statement, loading, error) no reducer global (store.ts).

Separação de Responsabilidades (UI):

Criação da aba ProjectCostTab.tsx dedicada exclusivamente à visualização analítica (Custo Total, Composição por Categoria e Tabela de Insumos Consolidados).

Refatoração da aba ProjectResourcesTab.tsx para operar estritamente como Livro Razão transacional (fluxo de aprovação/estorno).

Definições de Infraestrutura

Roteamento (API NestJS): Novo endpoint exposto GET /organizations/:orgId/resources/statement/:projectId via resources.controller.ts, protegido por AuthGuard.

Serviço de Rede (Next.js): Atualização do axiosInstance em resourceService.ts com o novo método getProjectStatement injetando implicitamente as credenciais da organização.

----------

